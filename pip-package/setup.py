from setuptools import setup

setup(name='easydistributions',
      version='0.1',
      description='Easy to use distributions, such as Gaussian, Binomial added with utility features',
      packages=['distributions'],
      zip_safe=False)